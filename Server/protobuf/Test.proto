syntax = "proto2";

package pbTest;

message Test {
    optional int32  id   = 1;
    optional string str  = 2;

        enum Type {
            unknown = 0;
            text = 1;
        }
    optional Type   type = 3;
};
